---
export interface RelatedContentItem {
  label: string;
  href: string;
  description?: string;
}

interface Props {
  items: RelatedContentItem[];
  heading?: string;
}

const { items, heading = 'Related Content' } = Astro.props;
---

{items.length > 0 && (
  <nav class="related-content" aria-label={heading}>
    <h2>{heading}</h2>
    <div class="related-grid">
      {items.map((item) => (
        <a href={item.href}>
          <strong>{item.label}</strong>
          <span>{item.description ?? `Continue to the ${item.label} page.`}</span>
          <b aria-hidden="true">-&gt;</b>
        </a>
      ))}
    </div>
  </nav>
)}

<style>
  .related-content { margin: 36px 0; }
  h2 { margin: 0 0 14px; font-size: 1.15rem; }
  .related-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 12px; }
  a { min-width: 0; min-height: 132px; display: flex; flex-direction: column; padding: 17px; border: 1px solid var(--border); border-radius: var(--radius); color: var(--ink); background: var(--surface); text-decoration: none; transition: transform .18s ease, border-color .18s ease, box-shadow .18s ease; }
  a:hover { transform: translateY(-2px); border-color: #dec3b4; box-shadow: var(--shadow); }
  strong { font-size: .94rem; }
  span { margin-top: 7px; color: var(--muted); font-size: .79rem; line-height: 1.5; overflow-wrap: anywhere; }
  b { margin-top: auto; padding-top: 10px; color: var(--accent); }
  @media (max-width: 760px) { .related-grid { grid-template-columns: 1fr; } a { min-height: 0; } }
</style>
